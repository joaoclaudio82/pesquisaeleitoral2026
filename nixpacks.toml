# psycopg[binary] não exige libpq do sistema — não sobrescrever nixPkgs (quebra Python).

[start]
cmd = "gunicorn --bind 0.0.0.0:$PORT --workers 2 --timeout 120 --graceful-timeout 30 --preload run:app"
